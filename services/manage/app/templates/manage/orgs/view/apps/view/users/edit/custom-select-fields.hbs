<form {{action "save" on="submit"}}>
  <fieldset disabled={{this.isActionRunning}}>
    <BsModal::Body>
      <div class="row">
        {{#each this.model.customSelectFieldAnswers as |fieldAnswer|}}
          <div class="col-sm-6">
            <CustomSelectField
              @dropdownClass="in-modal-dropdown"
              @label={{fieldAnswer.field.label}}
              @name={{fieldAnswer.field.name}}
              @active={{fieldAnswer.field.active}}
              @multiple={{fieldAnswer.field.multiple}}
              @answers={{fieldAnswer.answers}}
              @options={{fieldAnswer.field.options}}
              @disabled={{this.isActionRunning}}
            />
          </div>
        {{else}}
          <p class="text-muted">No custom fields have been created.</p>
        {{/each}}
      </div>
    </BsModal::Body>

    <BsModal::Footer>
      <FormElements::SubmitButton @class="btn btn-success" @isSaving={{this.isActionRunning}} />
    </BsModal::Footer>
  </fieldset>
</form>

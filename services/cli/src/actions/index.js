const buildSegments = require('./build-segments.js');
const createSegment = require('./create-segment.js');
const saveUser = require('./save-user.js');

module.exports = {
  createSegment,
  buildSegments,
  saveUser,
};
